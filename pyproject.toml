[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "llm-cli"
dynamic = ["version"]
dependencies = [
    "openai",
    "rich",
    "click",
]

[tool.hatch.version]
path = "llmcli.py"

[project.scripts]
llm-cli = "llmcli:cli"
